import { FiltroentregaPipe } from './filtroentrega.pipe';

describe('FiltroentregaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroentregaPipe();
    expect(pipe).toBeTruthy();
  });
});
